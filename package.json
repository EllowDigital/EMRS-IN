{
  "name": "emrs-in",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "main": "index.html",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev",
    "build": "netlify build",
    "build:functions": "esbuild \"netlify/functions/*.js\" --bundle --platform=node --outdir=netlify/functions/dist --format=cjs --target=node18",
    "lint": "echo \"No linter configured\"",
    "format": "echo \"No formatter configured\""
  },
  "dependencies": {
    "cloudinary": "^2.5.0",
    "postgres": "^3.4.4",
    "qrcode": "^1.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.11",
    "netlify-cli": "^23.9.5"
  },
  "author": "EllowDigital",
  "license": "MIT"
}
